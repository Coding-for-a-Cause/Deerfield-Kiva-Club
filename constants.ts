import path from 'path';

export const meetingMinutesDirectory = path.join(
  process.cwd(),
  'data',
  'meeting-minutes',
);

export const leadershipDirectory = path.join(
  process.cwd(),
  'data',
  'leadership',
);
